# GitKeyRouter v0.4.15 开发记录

## 版本目标

把测试项目迁移到 xUnit v3，并集中管理 NuGet 包版本，消除两个测试项目独立维护版本造成的漂移，同时保留现有 VSTest、TRX、锁定还原和发布冒烟流程。

## 实现范围

- 两个测试项目改用 `xunit.v3` 和 xUnit v3 可执行测试项目模型。
- 保留 `xunit.runner.visualstudio`，继续使用现有 `dotnet test` 和 TRX 工作流。
- 新增根目录 `Directory.Packages.props`，集中声明 `Microsoft.NET.Test.Sdk`、`xunit.v3` 和测试适配器版本。
- 从测试项目的 `PackageReference` 移除内联版本，普通还原和 `win-x64` 发布还原继续由锁文件固定完整依赖图。
- 显式记录 `xUnit1051` 暂时抑制；存量测试的专属取消/超时 Token 语义留到 v0.4.17 分析器阶段逐项核对，不在迁移提交中机械改写。

## 验证结果

- Core/Infrastructure：188 项通过，0 失败，0 跳过。
- App/发布冒烟：42 项通过，0 失败，0 跳过。
- 两个测试项目均生成 TRX，测试发现数量没有下降或重复。
- Release 解决方案构建：0 警告，0 错误。
- 普通解决方案与 `win-x64` 发布依赖均可使用锁文件还原。

## 提交边界

本版本 1 个提交：测试平台迁移、中央包管理、锁文件、版本号和文档。

## 开发状态

状态：**开发完成；正式标签、Windows 发布包和校验文件待生成**。
