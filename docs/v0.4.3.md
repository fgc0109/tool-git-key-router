# GitKeyRouter v0.4.3 记录（正式发布未确认）

## 记录性质

当前仓库没有 `.github/releases/v0.4.3.md`，远端 `project-base-mirror` 也没有 `v0.4.3` 标签。唯一可验证线索是 v0.4.4 发布说明写明“版本号从 0.4.3 提升到 0.4.4”。

因此，本文件只记录版本序列中的内部前序状态，不把 v0.4.3 声明为正式 Release，也不推测其功能、计划提交数量、测试数量或产物。

## 版本目标

未找到可靠资料，无法确认。

## 计划提交数量

未知。

## 测试用例

未知。

## 开发状态

状态：**可能存在过内部版本号，但正式发布未确认**。

后续只有在找到标签、发布产物、历史版本配置或其他可验证资料时，才应补充本记录。
